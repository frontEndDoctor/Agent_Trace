# Core Model Context Protocol dependencies
mcp>=1.0.0
fastmcp>=0.1.0

# Dependencies for Fairness Agent parity metrics & data analysis
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
fairlearn>=0.10.0

#To run the project locally, follow these steps:
# 1. Create a fresh virtual environment
python3 -m venv venv

# 2. Activate the environment (Mac/Linux)
source venv/bin/activate
# (Or on Windows: venv\Scripts\activate)

# 3. Install the dependencies from your file
pip install -r requirements.txt